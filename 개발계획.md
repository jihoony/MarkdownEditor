# Markdown Editor 개발 계획 (Implementation Plan)

`구현목표.md`를 바탕으로 한 단계별 데스크톱 마크다운 에디터 개발 계획입니다.

## 1단계: 프로젝트 초기화 및 기본 환경 구성 (완료)
- **기술 스택 확정**: 백엔드(Golang), 데스크톱 프레임워크(Wails v2), 프론트엔드(React/TypeScript 또는 Svelte 권장)
- **프로젝트 생성**: Wails CLI를 사용해 기본 템플릿 프로젝트 초기화
  - `wails init -n markdown-editor -t react-ts`
- **화면 기본 레이아웃 구성**: 
  - 좌측 사이드바 영역과 가운데 에디터 영역으로 화면 분할 (CSS Grid/Flexbox 활용)
  - 사이드바 숨김/표시 토글 기능(UI 버튼 및 `Ctrl + \` 단축키) 뼈대 구현

## 2단계: 핵심 에디터 및 렌더링 엔진 연동 (완료)
- **에디터 컴포넌트 연동**: CodeMirror 또는 Monaco Editor를 프론트엔드에 통합하여 소스코드 모드 구현
- **마크다운 렌더링 엔진 연동**: `marked` 또는 `markdown-it` 라이브러리를 사용하여 마크다운 텍스트를 HTML로 변환하는 뷰어 모드 구현
- **mermaid.js 연동**: 코드 블록 중 `mermaid` 언어를 감지하여 다이어그램으로 렌더링하는 커스텀 플러그인 작성
- **모드 전환 기능**: 소스코드 모드와 뷰어 모드(실시간 프리뷰 또는 단일 화면 토글 `Ctrl + E`) 간의 상태 동기화 및 UI 전환 구현

## 3단계: 파일 시스템 연동 및 백엔드(Golang) 기능 구현 (완료)
- **파일 관리 API 구현 (Go)**:
  - Wails의 `runtime` API를 활용하여 시스템 기본 파일 선택/저장 창 호출
  - 로컬 파일 읽기 및 쓰기 로직 구현
- **프론트엔드 통신**:
  - 새 파일(`Ctrl + N`), 열기(`Ctrl + O`), 저장(`Ctrl + S`), 다른 이름으로 저장(`Ctrl + Shift + S`) 기능 프론트엔드-백엔드 연동
- **자동 저장 기능**: 일정 시간 입력이 없을 시 현재 상태를 임시 저장하는 로직 구현
- **드래그 앤 드롭 파일 열기 지원**: 
  - 리눅스(Mint) 등 일부 환경의 웹뷰 호환성을 고려하여 HTML5 `onDrop` 이벤트 기반의 파일 읽기 기능 우회(Fallback) 구현

## 4단계: 좌측 사이드바 개요(TOC) 트리 구현 (완료)
- **Heading 추출 알고리즘**: 에디터 내용이 변경될 때마다 마크다운 텍스트를 파싱하여 `H1` ~ `H6` 태그를 추출 및 인덱싱
- **트리 뷰 UI 컴포넌트**: 
  - 추출된 Heading을 부모-자식 계층을 가지는 트리 구조로 변환
  - 접기/펼치기 동작이 가능한 UI 리스트로 렌더링
  - 항목 클릭 시 에디터(Monaco) 라인 이동 및 뷰어(미리보기 화면) 스크롤 동기화 구현

## 5단계: 편집 단축키 및 서식 기능 적용 (완료)
- **서식 단축키 구현**: 에디터 내 텍스트 선택 후 서식 적용
  - Heading 변환 (`Ctrl + 1~6`, 폴백 `Alt + 1~6`)
  - 굵게 (`Ctrl + B`), 기울임꼴 (`Ctrl + I`) : 스마트 토글(Smart Toggle) 적용 (선택 영역 감싸기 및 해제)
  - 코드 블록 삽입 (`Ctrl + Shift + C`) : 스마트 토글 적용
- **표(Table) 생성기 구현 (`Ctrl + T`)**: 가로/세로 행렬 숫자를 입력받는 모달(팝업) 창을 띄우고, 확인 시 마크다운 표 문법 자동 삽입
- **도움말 치트시트 (`F1`)**: 단축어 목록 팝업 및 툴바 툴팁 제공

## 6단계: 부가 기능 및 UI 고도화 (완료)
- **뷰어 스크롤 동기화**: 코드 영역과 뷰어 영역 간의 비율 기반(Proportional) 스크롤 양방향 동기화 완벽 적용
- **문서 내보내기 및 인쇄 (`Ctrl + P`)**:
  - `@media print` CSS를 활용하여 UI 요소 숨김 및 뷰어 내용만 깔끔하게 PDF로 인쇄/내보내기
- **UI/UX 개선**: 
  - 산뜻한 라이트 테마(White Tone) 전면 적용 (에디터 `f8f9fa`, 뷰어 `ffffff`)
  - `highlight.js` 연동을 통해 뷰어의 코드 블록 내 언어별 다채로운 구문 강조(Syntax Highlighting) 제공
  - Monaco Editor 자체에도 마크다운 문법(제목, 굵게 등) 강조 컬러 매핑 적용
- **단축키 충돌 방지**: 모든 이벤트를 가장 하위 `e.keyCode` 및 Monaco Native Command로 바인딩하여 데스크톱 환경(OS 호환성) 극대화

## 7단계: 보안성 강화 (Security Hardening) (완료)
- **프론트엔드 XSS 방어**: 마크다운 렌더러(`marked`) 결과물에 `DOMPurify` 라이브러리를 적용하여 악의적인 스크립트 실행(Cross-Site Scripting) 원천 차단
- **백엔드 파일 입출력 검증**: 
  - 시스템 임의 파일 읽기/쓰기(Arbitrary File Read/Write) 취약점을 막기 위해 프론트엔드에서 요청하는 파일 경로 검증 로직 추가
  - 확장자가 `.md`, `.txt`, `.markdown`인 안전한 텍스트 파일에만 접근(읽기/저장)이 가능하도록 화이트리스트 기반 접근 제어 구현

## 8단계: 고급 기능 (Advanced Features) (진행중)
- **1. 이미지 붙여넣기 및 자동 저장 (완료)**: 클립보드 이미지(`Ctrl+V`) 또는 파일 드래그 앤 드롭 시 로컬 `images/` 폴더에 이미지를 자동 저장하고 마크다운 문법으로 텍스트에 자동 삽입하는 기능 추가
- **2. 수식(Math) 렌더링 지원 (완료)**: KaTeX를 연동하여 수학/과학 수식 블록 렌더링 지원
- **3. 다크 모드(Dark Theme) 전환 (완료)**: 눈의 피로를 덜어주는 야간 모드 토글 지원
- **4. 파일 탐색 탭 (예정)**: 사이드바에 파일 탐색 탭을 추가하여 다중 문서 관리 기능 강화
- **5. 포커스 / 타자기 모드 (예정)**: 몰입형 글쓰기를 위한 현재 줄 하이라이트 및 타자기 식 고정 스크롤 구현
